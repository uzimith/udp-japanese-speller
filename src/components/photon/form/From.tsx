import * as React from 'react';
import cx from 'classnames';

export interface State {};

export interface Props {};

export default class From extends React.Component<Props, State> {

  constructor() {
    super();
  }

  render() {
    return (
        <span>
        </span>
      );
  }
}

